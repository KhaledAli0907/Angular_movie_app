import { PerecentagePipe } from './perecentage.pipe';

describe('PerecentagePipe', () => {
  it('create an instance', () => {
    const pipe = new PerecentagePipe();
    expect(pipe).toBeTruthy();
  });
});
